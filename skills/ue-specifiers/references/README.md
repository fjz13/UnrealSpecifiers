# References

Use these files as AI-readable summaries and workflows.

- `indexes/sources.index.md` is the generated routing catalog for all source documents. It keeps only short summaries, usage hints, status, and paths.
- Canonical detailed source documents live under `references/sources`.
- Source-level normalization progress is tracked in `audits/source-normalization-status.jsonl`.
